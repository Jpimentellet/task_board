import { Button } from '../Button'

const Actions = () => {
  return (
    <div className="actions">
      <Button
        text='Cancel'
        onClick={ () => {} }
      />
      <Button
        text='Save'
        onClick={ () => {} }
      />
    </div>
  )
}

export { Actions }